# ! [doc = " CURIE (Compact URI) resolution and namespace management"] # ! [doc = ""] # ! [doc = " This module provides comprehensive CURIE/URI resolution matching"] # ! [doc = " the Kapernikov LinkML implementation's namespace handling."] use linkml_core :: error :: { LinkMLError , Result } ; use linkml_core :: prelude :: * ; use once_cell :: sync :: Lazy ; use ml_model.extract_entities ("ex:Person" , "ex:Animal") . unwrap ()) ; } # [test] fn test_namespace_context () { let mut resolver = CurieResolver :: new () ; resolver . add_prefix ("global" , "http://global.org/") ; let mut context = NamespaceContext :: new (resolver) ; context . add_local_prefix ("local" , "http://local.org/") ; assert_eq ! (context . resolve ("local:Thing") . unwrap () , "http://local.org/Thing") ; assert_eq ! (context . resolve ("global:Thing") . unwrap () , "http://global.org/Thing") ; } }